# Review focus

The key question for this branch is not whether the world renderer is decorative. It is whether progress becomes spatially legible:

- CAMP at the beginning
- PATH / RIDGE through the middle
- GATE / RAID near completion

The renderer deliberately keeps the timer and session state external. `FocusUiState.progress` remains the source of truth.
